class Japan {
  constructor(
    capital,
    area,
    population,
    callingCode,
    region,
    language,
    timeZone,
    gdp,
    about,
    aboutSr,
    regionSr,
    jezikSr
  ) {
    (this.capital = capital),
      (this.area = area),
      (this.population = population),
      (this.callingCode = callingCode),
      (this.region = region),
      (this.language = language),
      (this.timeZone = timeZone),
      (this.gdp = gdp),
      (this.about = about),
      (this.aboutSr = aboutSr),
      (this.regionSr = regionSr),
      (this.jezikSr = jezikSr);
  }
}

export default Japan;
