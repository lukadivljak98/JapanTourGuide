class Sight {
  constructor(id, name, desc, imageUrl, latitude, longitude, descSr, nameSr) {
    (this.id = id),
      (this.name = name),
      (this.desc = desc),
      (this.imageUrl = imageUrl),
      (this.latitude = latitude),
      (this.longitude = longitude),
      (this.descSr = descSr),
      (this.nameSr = nameSr);
  }
}

export default Sight;
